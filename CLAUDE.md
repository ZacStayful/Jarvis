# Jarvis — Project Notes for Claude

## Session 2 Handoff — Monday Data Infrastructure

These IDs were generated by Session 2 (`setup/create-activity-log-board.js`
and `setup/create-management-leads-columns.js`) and are the source of truth
for all downstream sessions. They also live in `lib/monday-columns.ts` as
typed constants — prefer importing from there in code.

### Placeholder → real value mapping

When a session prompt or generated file contains a bracketed placeholder
from the list below, find-and-replace it with the value on the right.

| Placeholder | Value |
|---|---|
| `[ACTIVITY_LOG_BOARD_ID]` | `18414226069` |
| `[N8N_LOG_ACTIVITY_WEBHOOK]` | `https://stayful.app.n8n.cloud/webhook/log-activity` |
| `[WA_STATUS_COLUMN_ID]` | `color_mm3j9rd4` |
| `[WA_FIRST_SENT_COLUMN_ID]` | `date_mm3jbstp` |
| `[WA_LAST_REPLY_COLUMN_ID]` | `date_mm3jzdca` |
| `[WA_MESSAGES_SENT_COLUMN_ID]` | `numeric_mm3jrh70` |
| `[WA_REPLIES_COLUMN_ID]` | `numeric_mm3j1dff` |
| `[WA_TEMPLATE_COLUMN_ID]` | `text_mm3jr05s` |
| `[WA_CONVERSATION_COLUMN_ID]` | `long_text_mm3jj9fg` |
| `[WA_COMPLETED_COLUMN_ID]` | `boolean_mm3jfr4g` |
| `[DATE_FIRST_QUALIFIED_ID]` | `date_mm3jkn8w` |
| `[DATE_BECAME_CUSTOMER_ID]` | `date_mm3j751s` |

### Monday board IDs

- **Management Leads** — `5891626711`
- **Stayful Activity Log** — `18414226069`

### n8n workflows created in Session 2

- `stayful-log-activity` (id `VmhaGV2KvMyP6Lqt`)
  - Webhook: `https://stayful.app.n8n.cloud/webhook/log-activity`
  - Reusable sub-workflow. Every other workflow POSTs here to write
    one row to the Activity Log board.

- `stayful-status-change-logger` (id `Y6LWMYABgCFT2yEL`)
  - Webhook: `https://stayful.app.n8n.cloud/webhook/status-change`
  - Receives Monday automation pings when `status5` changes. Logs the
    change and sets `Date First Qualified` / `Date Became Customer` /
    `Was No Showed` where appropriate.

### Vercel env vars to add

```
ACTIVITY_LOG_BOARD_ID    = 18414226069
N8N_LOG_ACTIVITY_WEBHOOK = https://stayful.app.n8n.cloud/webhook/log-activity
```

### Manual tasks still required

- [ ] Add the two Vercel env vars above.
- [ ] Set up Monday automation: `When status5 changes → Send a webhook`
      pointing at `https://stayful.app.n8n.cloud/webhook/status-change`.
- [ ] In n8n, attach an HTTP Header Auth credential (sending
      `Authorization: <MONDAY_API_TOKEN>`) to all HTTP Request nodes in
      both workflows. n8n could not auto-assign because no existing
      credential matched the `Monday API` name.
- [ ] Verify Monday plan supports the `activity_logs` API
      (`/api/sales/activity` will return `activityLogsAvailable: true`
      once it's wired up in a later session).

## House rules

- Import Monday column IDs from `lib/monday-columns.ts`. Never inline
  the raw ID strings in route handlers or workflows.
- For Monday API calls in n8n: HTTP Request + GraphQL only. The native
  `mondayCom` node is banned for new workflows.
